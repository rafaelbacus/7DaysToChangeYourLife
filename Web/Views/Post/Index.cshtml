@model IEnumerable<Web.ViewModels.Post.PostViewModel>

<div class="post-index">
    <div class="row">
        <div class="col page-header">
            <h2 class="title">Posts</h2>
            <a class="btn btn-default btn-add" href="/Post/Add">Add</a>            
        </div>
    </div>
    <div class="row">
        @if(Model == null || Model.Count() == 0)
        {
            <div class="col">
                <h3>No recent posts.</h3>
            </div>
        }
        else 
        {
            foreach(var post in Model)
            {
                <div class="col-xs-12 col-xl-4 align-content-stretch">
                    <div class="post">
                        <a asp-controller="Post" asp-action="Edit" asp-route-id="@post.Id">
                            <h3 class="title">@post.Title</h3>
                            <p class="date">@post.RowCreatedDateTime.ToString("MMM dd, yyyy HH:mm:ss")</p>
                            <span class="content">
                                @Html.Raw(post.Content)
                            </span>
                        </a>
                    </div>
                </div>
            }
        }
    </div>
</div>